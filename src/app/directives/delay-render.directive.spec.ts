import { DelayRenderDirective } from './delay-render.directive';

describe('DelayRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayRenderDirective();
    expect(directive).toBeTruthy();
  });
});
